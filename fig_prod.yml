db:
  image: postgres:9.3
  volumes_from:
    - rails-pg-fig-sample-db-data
  ports:
    - 5432
web_prod:
  build: ./prod_build
  command: bundle exec unicorn -p 3000 -c ./config/unicorn.rb
  ports:
    - "3000:3000"
  links:
    - db