db:
  image: postgres:9.3
  volumes_from:
    - rails-pg-fig-sample-db-data
  ports:
    - 5432
webprod:
  image: evgenyneu/webprod
  command: bundle exec unicorn -p 3000 -c ./config/unicorn.rb
  ports:
    - "3000:3000"
  links:
    - db